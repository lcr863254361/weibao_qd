<#if PACKAGE_NAME && PACKAGE_NAME != "">package ${PACKAGE_NAME};</#if>
import com.orient.sysmodel.dao.IBaseDao;
/**
 * ${DESCRIPTION}
 * @author ${USER}
 * @create ${CURRENTDATE}
 */
public interface I${NAME}Dao extends IBaseDao{
}
